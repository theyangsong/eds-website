{
  "name": "@eds/website-showcase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --dir ../.. build:tokens && pnpm --dir ../.. build:components && pnpm --dir ../.. build:scenes",
    "dev": "vite",
    "prebuild": "pnpm --dir ../.. build:tokens && pnpm --dir ../.. build:components && pnpm --dir ../.. build:scenes",
    "build": "vue-tsc --noEmit && vite build",
    "build:pages": "vite build && cp dist/index.html dist/404.html && touch dist/.nojekyll",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@eds/website-components": "workspace:*",
    "@eds/website-scenes": "workspace:*",
    "@eds/website-tokens": "workspace:*",
    "highlight.js": "^11.11.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vue-tsc": "^2.2.0"
  }
}
